{
  "name": "minubooks.server",
  "main": "dist/index.js",
  "private": true,
  "version": "0.0.1",
  "packageManager": "yarn@3.2.3",
  "scripts": {
    "watch": "babel lib --watch --extensions \".js\" --out-dir dist --copy-files --no-copy-ignored",
    "dev": "nodemon dist",
    "build": "babel src --extensions \".js\" --out-dir dist --copy-files --no-copy-ignored",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/core": "^7.18.13",
    "@babel/preset-env": "^7.18.10",
    "babel-plugin-module-resolver": "^4.1.0",
    "babel-preset-minify": "^0.5.2",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.19",
    "request": "^2.88.2"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv-safe": "^8.2.0",
    "express": "^4.18.1",
    "helmet": "^6.0.0"
  }
}
